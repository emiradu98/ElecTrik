{
  "name": "electrik-frontend",
  "version": "1.0.0",
  "description": "A battery and energy platform",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "node-sass --output-style compressed -o dist/css src/scss",
    "serve": "browser-sync start --server --watch",
    "watch:css": "onchange 'src/scss' -- npm run scss",
    "start": "run-p serve watch:css"
  },
  "author": "Emilian Radu & George Badita",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "node-sass": "^4.13.0",
    "npm-run-all": "^4.1.5",
    "onchange": "^6.1.0"
  },
  "dependencies": {
    "leaflet": "^1.6.0"
  }
}
